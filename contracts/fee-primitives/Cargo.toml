[package]
name = "fee-primitives"
version = "0.1.0"
edition = "2021"
description = "Fee calculation primitives for AfroPay-Stellar transfers"

[dependencies]

[dev-dependencies]
# Property-based testing via a bounded fuzz loop (no external crates needed).
# If proptest is available, it can be added here:
# proptest = "1"
